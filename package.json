{
  "name": "AC_Tests",
  "version": "0.0.1",
  "scripts": {
    "install": "npm run update-webdriver",
    "update-webdriver": "webdriver-manager update",
    "e2e": "protractor protractor.config.js"
  },
  "devDependencies": {
    "cucumber": "^2.3.1",
    "protractor": "^5.1.2",
    "protractor-cucumber-framework": "^3.1.2"
  }
}
